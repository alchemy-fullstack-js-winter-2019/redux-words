import words from '../fixtures/words.json';


const intialState = {
    dictionary: words
};

export default function reducer(state = intialState, action) {

    // switch(action.type) {
    //     //some action
    //     default: 
    return state;
    // };
}

